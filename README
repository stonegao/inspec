with(Spec){
describe("A shared Spec", function(){
  it("should test shared", function(){
  
  })
})

describe("Foo", function(){
  include({
    helper1 : function(){
    }
  })
  
  describe("Bar", function(){
  })
  
  beforeEach(function(){ ... })
  
  afterEach(function(){ ... })
  
  beforeAll(function(){ ... })
  
  afterAll(function(){ ... })
  
  it("should do blah", function(){
  
  })
  
  it("should be moo", function(){
  })
  
  itShouldBehaveLike("A shared Spec")

})}

Sepc:
describe
it
beforeEach
afterEach
beforeAll
afterAll

ExampleGroup
describe
it
beforeEach
afterEach
beforeAll
afterAll

Example
run
